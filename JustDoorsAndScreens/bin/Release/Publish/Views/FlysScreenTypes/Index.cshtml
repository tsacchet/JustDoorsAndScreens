@model IEnumerable<JustDoorsAndScreens.FlysScreenType>

@{
    ViewBag.Title = "Index";
}

<div class="card border border-dark shadow">

    <div class="card-header bg-light border-dark">
        <h4 class="text-dark"><strong>FlyScreen Type List</strong></h4>
    </div>

    <div class="card-body">


        <div class="table-responsive">
            <table class="table table-sm table-striped">
                <thead class="thead-dark">
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.FlyScreenName)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.FlyScreenName)
                            </td>
                            <td>
                                @Html.ActionLink("Edit", "Edit", new { id = item.FlyScreenTypeID }) |
                                @Html.ActionLink("Details", "Details", new { id = item.FlyScreenTypeID }) |
                                @Html.ActionLink("Delete", "Delete", new { id = item.FlyScreenTypeID })
                            </td>
                        </tr>
                    }

</table>
        </div>
    </div>

    <div class="card-footer">
        @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-primary" })
    </div>

</div>
